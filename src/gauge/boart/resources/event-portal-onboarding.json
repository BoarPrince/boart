{
  "activationCode": "${store:ob-companyId}",
  "Taker": {
    "companyId": "${store:ob-companyId}",
    "subsidiaryId": "${store:ob-carrierId:=${generate:uuid}}",
    "name": "${generate:s:fake:company:name}-${generate:s:random:4}",
    "taxId": "${generate:random:8}",
    "address": {
      "street": "${generate:t:fake:address:street}",
      "postCode": "${generate:t:fake:address:zipCode}",
      "city": "${generate:t:fake:address:city}",
      "countryCode": "DE"
    },
    "customerId": "${generate:uuid}",
    "JitpayCustomerId": "K${generate:s:random:4}",
    "NameZusatz": "GmbH_de",
    "Telephone": "+49 1743284130",
    "UserDetail": {
      "userId": "${store:ob-userId}",
      "firstName": "${generate:t:fake:name:firstName}",
      "lastName": "${generate:t:fake:name:lastName}",
      "email": "${store:ob-email:=jitpaytest+ob${generate:t:random:4}@gmail.com}"
    }
  },
  "GiverTakerMapping": {
    "TakerId": "K${generate:s:random:4}"
  }
}
