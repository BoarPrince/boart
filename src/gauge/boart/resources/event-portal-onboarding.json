{
  "activationCode": "${generate:t:tpl:company.id}",
  "Taker": {
    "companyId": "${generate:t:tpl:company.id}",
    "subsidiaryId": "${store:ob-carrierId:=${generate:uuid}}",
    "name": "${generate:t:tpl:company.ob-name}",
    "taxNumber": "${generate:random:8}",
    "address": {
      "id": "${generate:uuid}",
      "street": "${generate:t:fake:address:street}",
      "postCode": "${generate:t:fake:address:zipCode}",
      "city": "${generate:t:fake:address:city}",
      "countryCode": "DE"
    },
    "customerId": "${generate:uuid}",
    "JitpayCustomerId": "K${generate:s:random:4}",
    "NameZusatz": "GmbH_de",
    "Telephone": "+49 1743284130",
    "UserDetail": {
      "userId": "${generate:t:tpl:user.id}",
      "firstName": "${generate:t:tpl:user.firstname}",
      "lastName": "${generate:t:tpl:user.lastname}",
      "email": "${generate:t:tpl:user.email}"
    }
  }
}